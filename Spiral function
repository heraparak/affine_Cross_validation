import turtle

def spiral(sides, turn, color, width):
    t = turtle.Turtle()
    t.speed(0)
    t.color(color)
    t.width(width)
    for n in range(sides):
        t.forward(n)
        t.right(turn)

spiral(30, 50, "cyan", 3)
